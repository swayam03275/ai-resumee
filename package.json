{
  "name": "resume-builder-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend/resume && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend/resume && npm run dev",
    "build": "cd frontend/resume && npm install && npm run build",
    "vercel-build": "npm install --prefix frontend/resume && npm run build --prefix frontend/resume && cp -r frontend/resume/dist public || xcopy /E /I /Y frontend\\resume\\dist public"
  },
  "workspaces": [
    "backend",
    "frontend/resume"
  ]
}
