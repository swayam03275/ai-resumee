{
  "name": "resume-builder-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/resume && npm install && npm run build"
  },
  "workspaces": [
    "backend",
    "frontend/resume"
  ]
}
